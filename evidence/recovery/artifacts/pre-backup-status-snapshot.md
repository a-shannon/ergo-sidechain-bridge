# Pre-backup status snapshot

Command evidence: `npm run backup:snapshot` captured the pre-backup status snapshot from the synthetic source SQLite database.

Result: pre-backup snapshot JSON is linked as `artifact://recovery/artifacts/pre-backup-snapshot-2026-05-31-99e98fff.json`.
