# Post-restore status snapshot

Command evidence: `npm run backup:snapshot` captured the post-restore status snapshot from the isolated restore SQLite database.

Result: restored snapshot JSON is linked as `artifact://recovery/artifacts/restored-snapshot-2026-05-31-99e98fff.json`.
