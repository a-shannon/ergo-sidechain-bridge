# State: runtime artifact hygiene

State evidence: `npm run backup:compare` local snapshot comparison output and git hygiene review.

Measured value: `snapshot read-only; confirm git status clean or backups ignored`.
