# Relayer Tests Evidence

Evidence:
- `npm.cmd run check` invoked `vitest run`.
- Vitest completed with 89 test files passing and 3,089 tests passing.
- Publication remains blocked if relayer test evidence is missing or fails.
