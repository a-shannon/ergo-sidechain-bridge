# TypeScript Build Reproducibility Evidence

Evidence:
- `npm.cmd run check` invoked `tsc`.
- TypeScript build completed successfully.
- Publication remains blocked if TypeScript build evidence is missing or fails.
