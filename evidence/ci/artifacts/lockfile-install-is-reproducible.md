# Lockfile Install Reproducibility Evidence

Evidence:
- `npm.cmd ci` completed from the checked relayer lockfile.
- Install result was pass, exit code 0.
- Publication remains blocked if lockfile install evidence is missing or fails.
