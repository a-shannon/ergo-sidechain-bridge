# npm ci Evidence

Command: `npm.cmd ci`

Result: pass, exit code 0.

Summary:
- Dependencies were installed from the checked lockfile.
- 143 packages were added and 144 packages were audited.
- The command reported 7 moderate npm audit findings; this artifact only supports clean-checkout reproducibility and does not close dependency/security review gates.
