# Broadcast Enablement Decision Evidence

Decision-specific external integration evidence: broadcast enablement requires `BRIDGE_BROADCAST_ENABLED=true` only after readiness review and explicit opt-in.
