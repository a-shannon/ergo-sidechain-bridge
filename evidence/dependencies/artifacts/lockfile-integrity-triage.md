# Lockfile Integrity Triage

Method: package-lock review and `npm ci`.

Finding: zero open critical/high vulnerabilities for the lockfile integrity decision. The lockfile is retained as the reproducibility source.
