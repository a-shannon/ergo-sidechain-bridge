# ContextExtension Guard Evidence

Decision: keep the signer fail-closed.

Evidence: the sigma-rust ContextExtension serializer path remains guarded. Production-ready claims blocked until upstream signer release and JVM/node conformance evidence are validated. Testnet production-candidate claims blocked until upstream signer release and JVM/node conformance evidence are validated.
