# ergo_avltree_rust Scope

Reviewed source: locked Rust tree and WASM AVL crate.

Risk reviewed: `ergo_avltree_rust` proof behavior must remain compatible with JVM AVL expectations. `cargo tree --locked` and `npm run wasm:test` provide the current dependency evidence.
