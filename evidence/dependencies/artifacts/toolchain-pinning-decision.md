# Toolchain Pinning Decision

Decision: keep wasm-pack, Rust, Node.js, npm, and package-lock toolchain inputs pinned.

Rationale: reproducible WASM and TypeScript evidence depends on stable toolchain and lockfile inputs.
