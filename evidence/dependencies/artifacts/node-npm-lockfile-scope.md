# Node.js and npm Lockfile Scope

Reviewed source: package lock and npm install path.

Risk reviewed: Node.js, npm, transitive dependencies, and package-lock reproducibility were reviewed through `npm ci`, `npm audit --omit=dev`, and lockfile inspection.
