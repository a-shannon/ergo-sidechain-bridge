# Completed Current SPVTracker Contention Evidence - 2026-06-25 - ca56646f

Command:

```powershell
npm run showcase:lanes
```

Result: PASS / exit code 0.

Evidence:

- SPVTracker remains shared today.
- Shared input overlap is SPVTracker only.
- SPV tracker contention is the remaining full-parallel settlement bottleneck.

Boundary:

- This is offline planner evidence.
- It does not prove pre-ingest or tracker sharding mitigation.
